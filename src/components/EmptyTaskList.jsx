import React from 'react';

const EmptyTaskList = () => {
    return (
        <div>
            You todo list is empty. Please, add a new task, using the following button:
        </div>
    );
};

export default EmptyTaskList;